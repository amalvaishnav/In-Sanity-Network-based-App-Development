<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link
      rel="stylesheet"
      type="text/css"
      href="./../assets/stylesheet/styles.css"
    />
    <title>Home</title>
  </head>
  <body>
    <div class="container">
      <%- include('partials/header.ejs', {loginFlag: loginFlag}) %>
      <div class="main mainConnection">
        <h2>Login now</h2>
        <h3 class="invalid"><%= invalidText %></h3>
        <form id="connectionForm" method="POST" action="/myConnections">
          <label for="userName">Username </label>

          <input id="userName" type="text" name="userName" placeholder="UserName" />
          <br />
          <label for="password">Password </label>
          <input
            id="password"
            type="password"
            name="password"
            placeholder="password"
          /><br />
          <input class="butSubmit" type="submit" value="Submit" />
        </form>
      </div>
      <% include partials/footer.ejs%>
    </div>
  </body>
</html>
